# 2026-08-19 工作日志

## 项目整体架构评审（软件团队 · 架构师高见远）
- 用户反馈三大痛点：①前端模块复用不足 ②输入输出规则冗余 ③视频处理速度慢
- 架构师通读 16 个 tab + 23 个 core 模块，输出评审报告，关键结论（均有文件证据）：
  - 17 个手写 QThread Worker 类散落各处；QLineEdit 主题修复写三层（styles.py FIX_LAYOUT_QSS → fission LINEEDIT_QSS → 逐控件 setStyleSheet）
  - 视频扩展名列表重复 19 处；ffprobe 包装 4 份；9:16/blur-pad 实现 3 份不一致；setsar=1 手工重复 6 处
  - 性能：仅 fission 用 NVENC（6.3×），其余全部 libx264 硬编码 + 串行；concat 双重重编码；无 -progress 解析；无缓存/断点
  - requirements.txt 的 moviepy 实际零使用（全 subprocess ffmpeg）
- 建议：抽前端公共组件（BaseTab/path_row/task_progress/log_view/result_table/BaseWorker/validators，gui/common/）+ 后端公共层（ffmpeg_runner/ffmpeg_probe/encoder_policy/output_spec/path_utils/video_collect）
- 性能优化排序：①EncoderPolicy 全局接入 ②音频 copy ③批量并发 ④probe 缓存 ⑤去双编码 ⑥进度解析 ⑦断点续跑
- 路线图 3 阶段：①公共层落地只增不改（约1-2周）②tab 分批迁移（约2-3周）③性能专项先建基准（约1-2周）
- 不做：UI 框架重写 / moviepy 替换 / QSV 优先 / 任务持久化
- 交付：docs/architecture-review-2026-08-19.html（自包含深色报告，含现状 vs 目标架构图）
- 待用户决策：是否进入阶段 1 实施（若实施，建议先试点 video_resize_tab）

## 外部报告对比（Trae Work 生成的 arch-optimization-report）
- 用户提供第二份评审报告，结论高度一致（缺公共层 + fission 能力被锁），新增 4 个我们漏掉的盲区：
  ① 5 套 ASR 实现 18 类方法重复（可消 300-400 行）② main_window 注册已不同步（现存 bug，closeEvent 漏 settings_tab）
  ③ 清空元数据只有 fission 做（其他模块产物保留 Lavf 痕迹，投千川有被识别风险）④ styles.py 公共常量零引用（53 处内联样式）
- 另细化：停止机制 5 种变体、config bool 序列化 3 种并存、os.walk 复制 13 处、cosyvoice_server 为最佳解耦范式
- 路线图微调：采纳"core 层先行"排序（P0 修 bug → P1 core 公共化 → P2 GUI 基类 → P3 深化）

## P0+P1 重构实施（用户确认范围：P0+P1 先行）
- 流程：PM 许清楚出 PRD（docs/refactor-prd-p0p1.md）→ 架构师高见远出设计（docs/refactor-design-p0p1.md + 类图/序列图）→ 工程师寇豆码实施中**团队消息通道断开**（SendMessage Not in a team，工程师 agent 停摆）→ 主理人接管完成剩余实施
- 全部 9 个提交（本地 main，**push 未完成**——PortableGit credential-helper-selector 无头环境卡死，需用户在 IDE/终端手动 push）：
  - e7dad24 P0 注册表：gui/tab_registry.py（TABS 16 项 + stop_tab_threads vars 扫描 QThread）；main_window 删除 4 处平行列表；LoginWorker 补 stop()
  - cdf5b38 公共层四件套：core/encoder.py（NVENC 探测+缓存+全局回退）+ core/ffmpeg_runner.py（CREATE_NO_WINDOW/进程表/超时清理/回退）+ utils/media_utils.py + utils/path_utils.py + scripts/compare_media.py
  - 49dc158 试点 resizer（四件套完整切换，compare_media 5 素材 PASS）
  - 4a1e19f 裂变接入公共层（净删 85 行，行为不变：清元数据/随机comment/时间戳/中断保留）
  - 4fb81ef video_utils 8 处编码段 + cut 系列补 setsar=1
  - 497d88a concatenator 3 处 + normalize_input_path/get_videos/_probe_video 工具导入单点化（**坑：gui/video_concat_tab.py 还引用 normalize_input_path，已改为从 utils.path_utils 导入**）
  - deb2176 keyword_remover（VIDEO_EXTS/collect_videos re-export）
  - 8f5ed14 subtitle_tab（**行尾 CRLF→LF 显示全量 diff，实际逻辑 2 处**——与 main_window 之前统一 LF 一致）
  - aba201f docs 归档
- 验证：py_compile 全过、18 模块 import 冒烟、main.py 启动冒烟（16 tab 无 traceback）、裂变/切片/拼接/尺寸实测全部 SAR=1:1
- **⚠️ 关键环境发现：本机无 NVIDIA GPU**（只有 Intel Arc + 向日葵虚拟显示器），NVENC 探测自动回退 libx264；QSV 实测慢软件 3.4×；≥2× 提速验收需 NVIDIA 机器部署后执行（自动探测已就位）
- 遗留：_p0p1_tmp/ 临时验证目录（几十 MB，可删）；push 未完成（9 提交待推送）
