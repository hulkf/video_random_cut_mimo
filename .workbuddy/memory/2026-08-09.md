# 2026-08-09

## 修复：项目启动崩溃（astor 包损坏）
- 现象：`python main.py` 启动即崩溃，Traceback 指向 `paddle -> paddle.distributed -> ... -> astor` 导入链，最终报 `ModuleNotFoundError: No module named 'astor.node_util'`
- 根因：用户级 site-packages（`C:\Users\91682\AppData\Roaming\Python\Python312\site-packages`）里的 astor 0.8.1 包文件不全 —— 缺 `node_util.py`（code_gen.py 依赖它），疑似被清理工具/杀软误删或安装损坏
- 修复：`D:/Anaconda/python.exe -m pip install --force-reinstall --no-deps astor==0.8.1` 强制重装后正常
- 验证：`import paddleocr` OK；main.py 后台跑 15 秒仍存活（GUI 正常）
- 附注：pip show 时大量包报 "invalid metadata entry 'name'" 警告，用户 site-packages 里很多 dist-info 元数据损坏，暂不影响运行，未处理
