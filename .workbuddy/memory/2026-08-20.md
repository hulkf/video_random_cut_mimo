# 2026-08-20 工作日志

## P2 GUI 基类化（继续重构）
- 背景：用户确认继续推进 P2（前端复用不足的根治）。团队消息通道已断开（SendMessage 不可用），主理人直接实施
- 交付（commit 2d81f0c，6 文件 +375/-129）：
  - **gui/common/base_worker.py**：BaseWorker(QThread) 统一信号 progress(int,int,str)/finished(list)/error(str)；run() 统一 try/except；stop()/request_stop()/stopped() 与 tab_registry 配合
  - **gui/common/base_tab.py**：BaseTab(QWidget) start_worker(防重入+信号连接+set_busy)/is_busy/set_busy/on_worker_finished/on_worker_error 默认实现 + load/save_config 骨架
  - **gui/common/path_row.py**：PathRow 路径选择行（folder/file/files 三模式 + 浏览 + on_change 回调）；LINEEDIT_QSS 唯一一份输入框修复样式（qt-material 坑单点化）
  - **gui/common/progress_panel.py**：ProgressPanel 进度条+百分比+状态行统一接口
  - **gui/video_resize_tab.py**：试点迁移（worker 继承 BaseWorker、tab 继承 BaseTab、PathRow/ProgressPanel），行为保持（弹窗/进度文案/表格一致）
- 验证：py_compile + import + main.py 启动 16 tab 无 traceback + resize 实测 SAR=1:1
- 坑：git add gui/common/ 时混入 __pycache__/*.pyc（新目录未被忽略），用 git rm --cached + commit --amend 修正
- 待办：其余 15 个 tab 逐步迁移（按低频→高频分批）；push 仍未做（凭据问题，本地共 11 个待推送提交）

## P2 第二批：页面批量迁移（3 个）
- commit ee8608c（slice）：SliceWorker 继承 BaseWorker（保留 pause/resume/stop，_stop 统一为 stopped()）；SliceTab 继承 BaseTab；输出行换 PathRow（输入行保留文件/文件夹复合浏览）；进度区换 ProgressPanel（保留"总数刻度"语义 setMaximum(total)）
- commit 3676632（screenshot）：ScreenshotWorker 继承 BaseWorker；Tab 继承 BaseTab；两路径行换 PathRow；进度区换 ProgressPanel；browse_folder/browse_output 改为转发 _browse()（兼容外部调用）；清理无用 import
- 已迁移累计：video_resize / slice / screenshot 3 个页面；全部 py_compile + import + main.py 启动冒烟通过
- 迁移模式固化：worker 继承 BaseWorker（run→work、信号 (int,int,str)）→ tab 继承 BaseTab（start_worker 防重入 + set_busy + on_worker_* 回调）→ 路径行换 PathRow（特殊复合浏览保留）→ 进度区换 ProgressPanel（语义可保留原刻度）
- 待迁移剩余 13 个 tab：text_recognition/face_detection/audio_mix/video_mix/video_concat/keyword_remove/voice_clone/kaipai_cloud/video_fission/video_enhance/video_download/subtitle/settings（中高频后置）

## P2 完成：全部 16 页迁移完毕（用户要求"全部做完"）
- 手工迁移：text_recognition、face_detection（commit 95cf727）
- 子代理批量迁移 10 页（commit 3827614）：audio_mix/video_mix/video_concat/keyword_remove/voice_clone/kaipai_cloud/video_fission/video_enhance/video_download/subtitle
- 迁移规范（可复用）：worker QThread→BaseWorker（信号统一 (int,int,str)，emit 补空串；**run 保留子类实现**最小风险）→ tab QWidget→BaseTab（删 self.worker=None；start_xxx 用 start_worker；回调改 on_worker_*；set_busy 管按钮状态）
- 特殊适配：video_mix/concat 信号 (int,int,str,int) 保留；download 双 worker 仅 DownloadWorker 基类化（LoginWorker 保持 QThread，P0 已补 stop）；subtitle 内部 OllamaChecker 不动；fission 路径行/样式/多输入源未动；settings 无后台任务不迁移；mix_tab.py 废弃未注册不动
- **全量验证：gui/*.py 全编译通过 + 16 tab import OK + main.py 启动冒烟通过**
- 累计本地提交约 16 个未 push（凭据问题待手动 push）

## 全量修复（团队 SOP：工程师 5 批 + QA 2 轮，6a6d1bf..4bd6c2a，已 push）
背景：架构师评审 + GLM 对比分析合并出修复清单，用户要求"一次性全部改完不停顿"。团队 software-fix-all 执行。
- **批次1 6a6d1bf fix(security)**：gui/config.py 密钥独立存储 config.local.json（get_secret/set_secret）+ 模块级 _cache 缓存 + 原子写（mkstemp+os.replace）+ bool 统一小写序列化（兼容旧 "True"）；config.json kaipai api_key/secret_key 清空为 ""（**已泄露进 git 历史，需用户去开拍平台重置密钥**）；kaipai_cloud_tab/settings_tab 改 get_secret/set_secret；新增 .gitignore（config.local.json/__pycache__/*.pyc/_p0p1_tmp/TaobaoVideoDownloader.exe_extracted/_whisper_transcribe.py）
- **批次2 4608376 fix(process)**：ffmpeg_runner 修复进度模式 stderr 丢失 + 新增 kill_all_ffmpeg()；video_concatenator(9)/keyword_remover(2)/screenshot(2)/video_utils(13)/mixer/text_detector 全部接 run_ffmpeg/run_ffmpeg_with_fallback（ffprobe 切 media_utils）；5 个 ASR 模块加 CREATE_NO_WINDOW（读 wav stdout 无法接 run_ffmpeg，仅加黑窗消除）；subtitle_tab/screenshot_tab GUI 层 ffmpeg 下沉；main_window closeEvent 加 kill_all_ffmpeg() 兜底
- **批次3 577532d refactor(contract)**：信号契约统一——concat/mix progress 4参→3参+sub_progress 专用信号、download progress(int,int,int)→标准+percent、enhance finished(dict)→finished(list)+summary；删子类重复信号定义；13 个 Worker 主循环轮询 stopped()；audio_mix 双停止机制统一父类 + Tab 私有访问改公开方法；BaseTab on_worker_finished 置 self.worker=None + start_worker 兜底清理 lambda；gui/common/__init__ 显式导出
- **批次4 3e2177e refactor(p1)**：video_fission_tab LINEEDIT_QSS 统一从 path_row 导入；删 gui/mix_tab.py；实际删除 _p0p1_tmp(389M)/TaobaoVideoDownloader.exe_extracted(57M)；requirements 移除 moviepy；删 FFmpegCommand 死代码；video_fission except RuntimeError→FFmpegError + docstring NVENC→libx264
- **批次5 3b54fd8 feat(p2)**：ProgressPanel dual 双进度模式（sub_bar 可选，不破坏单进度接口）；concat/mix 手写双进度改用 ProgressPanel；PathRow 拖拽（setAcceptDrops/dragEnterEvent/dropEvent）+ 多文件完整列表 paths()
- **返工 4bd6c2a fix(fission)**（QA 第 1 轮抓到阻断 Bug）：`gui/video_fission_tab.py` 原 `stopped = pyqtSignal(list)` 与 BaseWorker.stopped() **方法同名**，PyQt5 信号遮蔽方法 → 进度回调必崩、UI 弹错。改名为 partial_results（3 处同步）。**教训：PyQt 子类信号严禁与基类方法同名！**
- **QA 建议项全处理**：ffmpeg_runner 进度模式补 watchdog 超时（kill+删半成品+FFmpegError timed_out）；config get_config 对 list/dict 叶子浅拷贝防污染缓存；base_tab on_worker_error 置 None；settings/face_detection 删无用 import；subtitle whisper 子进程补 CREATE_NO_WINDOW
- 验证：全量 py_compile + import gui.main_window 通过；QA 27 项检查 26 通过 + 第 2 轮复验通过
- **需要重启应用**：所有改动在 Python 源码层
- 遗留 dirty（非本次）：运行中 App 写空的 config.json video_mix 字段、.workbuddy/memory/MEMORY.md
- **push 经验**：本会话 bash stdout 被 Qt 离屏测试搞坏，git push 挂起 → 改用 PowerShell 完成 push（wincred 凭据）

## 第二轮架构评审（软件开发团队 · 架构师高见远）
- 触发：用户问「重构完了架构还需不需要再优化」；判定为「架构评审」部分工作流，只调度 software-architect，不走 PRD/编码
- 交付：`docs/arch-review-2026-08-20-round2.md`（196 行，含维度评分表 + 架构债 D1-D10 + Mermaid 分层图 + P0/P1/P2 建议 + 明确不建议做的事）
- **结论**：地基已稳（公共层/注册表/线程标准化 9 分），剩余是「减重收口」而非地基问题，**不建议大拆大改**；P0 无
- 新发现的债（此前未记录）：
  - **D1 依赖偏离（ROI 最高）**：`sherpa_onnx` 被 fireredasr.py:405 / onnx_asr.py:73,89 / sherpa_asr.py:12 import 但**不在 requirements.txt**；torch/torchaudio 只被 `_whisper_transcribe.py:61` 与 `services/cosyvoice_server.py:51` 用（可选子进程）→ 应移到 requirements-optional.txt
  - **D2 SenseVoice 双实现**：字幕页走 sherpa_asr（sherpa_onnx 库）、去关键词页走 onnx_asr→sensevoice_onnx（纯 onnxruntime），同能力两套；5 个 ASR 模块共约 2116 行
  - **D4 反向依赖**：`utils/video_utils.py:5` import core.ffmpeg_runner（utils→core，违反 gui→core→utils 单向）
  - **D5 跨 tab 耦合**：`keyword_remove_tab.py:17` import subtitle_tab 的 FIREMODELS_DIR/FUNASR_DIR/SENSEVOICE_DIR
  - **D6 VIDEO_EXTS 字面量残留**：subtitle_tab.py:57、face_detection_tab.py:120 等仍手写扩展名元组
  - **D10 crf 默认不一致**：ffmpeg_runner.py:285 默认 crf=20，canonical 为 23
- 核查确认：重编码路径均已接入 run_ffmpeg_with_fallback→get_encoder（铁律基本贯彻）；`gui/mix_tab.py` 已不存在；无 tests/CI/打包脚本
- 待用户答复 Q1（哪些 ASR 可删）/ Q2（torch demucs 是否设为可选）/ Q3（crf 铁律是否收紧到 100%）后再开团实施 P1
- 本轮**未动任何代码**，无需重启应用
- 顺带：`.workbuddy/memory/MEMORY.md` 超限 → 明细归档到 `archive/MEMORY-detail-2026-08-20.md`，主文件压缩到约 1700 字符

## 三份复盘交叉验证 + 详细规划
- 输入：round-2 + GLM 迁移完整度 + 千问体检报告 v2，三方视角互补无一份全对
- 交付 2 文档：`docs/arch-three-way-review-2026-08-20.md`（交叉对照表）+ `docs/optimization-roadmap-2026-08-20.md`（总结分析+4阶段详细规划）
- 核实落定：千问 2.1 fallback 盲目重试（捕获所有 FFmpegError 含超时→全局废 NVENC+重跑）= 真 P0，推翻 round-2「无 P0」；2.2 跨 tab 进程互杀（_procs 全局 set）属实；2.3 CosyVoice 无 stop/closeEvent 不清理属实但列 P1（本机跑不起来）；2.5 audio_mix 停止不复位属实，根因更深——AudioMixWorker 覆盖 run() 绕过 BaseWorker 统一异常处理 + 停止不 emit → BaseTab 复位全挂 finished/error 信号
- GLM 4 条 claim 全属实（face_detection _browse 崩溃、VIDEO_EXTS 13 处散落、utils→core 反向依赖、keyword_remove 跨 tab 耦合）
- 反对：GLM「ConfigStore 是缺口」= 过度设计；千问 2.3 列 P0 偏重
- 规划 4 阶段：① 立刻做 6 项（fallback 收窄/face_detection 崩溃/VIDEO_EXTS 收口/audio_mix 复位/仓库卫生/进程互杀）② 本周（CosyVoice stop/开拍云移 worker/demucs track/text_detector 懒 import/常量提公共/反向依赖收口）③ 短期（BaseWorker.run 根治统一 emit/ASR 收敛/内存分块/logging）④ 中期（taobao 分区/冒烟脚本/路径收敛/wink 8元组/crf 统一）
- 待用户答 Q1（哪些 ASR 可删）/Q2（torch 可选化）/Q3（get_encoder 收紧 100%）后推进阶段 3
- 本轮未动任何代码，无需重启

## P1+P2 落地执行 + 全部 push（用户指令"一次性解决"）
- 用户确认规划后下令执行；按"解决不了就跳过"原则，P1 全做 + P2 全做，P3/P4 暂缓
- **P1 全部（6 项）**：
  - P1.1 `core/encoder.py`+`core/ffmpeg_runner.py`：NVENC fallback 收窄为仅"会话受限且非超时"才回退 libx264（推翻千问 2.1 盲目重试 P0）；复用既有 `is_session_limit()`
  - P1.2 `gui/face_detection_tab.py`：裸 QLineEdit→PathRow，删 `browse_folder`（原调 PathRow._browse 必崩）
  - P1.3 VIDEO_EXTS 常量单点化（13 处→utils/media_utils 一处，commit b048c67）
  - P1.4 `gui/audio_mix_tab.py`+`gui/common/base_worker.py`：AudioMixWorker.run 捕获 InterruptedError→emit finished([]) 复位 UI；BaseWorker.run finally 兜底 emit finished([]) 防 tab 卡死
  - P1.5 仓库卫生：`*.log`/`taobao_auth.json`/`h5api_test.txt`/tb_*.html/arch-optimization-report 进 .gitignore；`git rm --cached core/sherpa_asr_error.log` 停止跟踪运行时日志；纳入 services/cosyvoice_server.py + 两份评审文档
  - P1.6 `core/video_fission.py`+`core/ffmpeg_runner.py`：进程追踪加 owner 分组 + `terminate_owner()`，fission 停止只杀本组，根治跨 tab 互杀
- **P2 全部（6 项）**：
  - P2.1 `core/voice_clone.py`+`gui/voice_clone_tab.py`：新增 CosyVoiceService.stop() 清理残留进程 + try/finally 调用
  - P2.2 开拍云移 worker：经核查 KaipaiWorker(BaseWorker) 早已完成 → 跳过
  - P2.3 `core/audio_utils.py`：demucs 由裸 subprocess.run 改为 Popen+track_proc+CREATE_NO_WINDOW+communicate(timeout=600)
  - P2.4 `core/text_detector.py`：移除顶部 paddleocr import，改 `_init_worker`/`__init__` 内 lazy import（规避重导入痛点）
  - P2.5 `core/model_dirs.py` 新建单点收敛 FIREMODELS_DIR/FUNASR_DIR/SENSEVOICE_DIR；subtitle_tab/keyword_remove_tab 改用，切断 gui→gui 耦合
  - P2.6 `utils/video_utils.py`→`core/video_utils.py`（git mv 保留历史）；8 处调用方引用全部更新，反向依赖收口
- **分组提交 6 个（全部已 push 到 origin/main）**：3bf1100(P1.1/1.6)→898918c(P1.2)→18dad08(P1.4)→b048c67(P1.3)→4035826(P2)→1ee2956(P1.5)。remote main = 1ee2956
- **验证**：全部 P2 文件 py_compile 通过；grep 全仓无 `utils.video_utils` 残留（仅 media_utils 注释）；import core.model_dirs/core.video_utils OK
- **有意排除**：`config.json` 79 行本地 UI 设置变更（用户素材路径等）保持未提交，避免覆盖仓库默认配置
- **需重启运行中的 App**：core/ 与 gui/ 大量源码改动，已经在跑的进程必须重开才能加载新模块
- **遗留/P3/P4 暂缓**：统一 logging、大视频内存分块、taobao 1437 行巨石分区、冒烟脚本、路径全收敛、wink 8 元组标准化、crf 统一均按规划后置；Q1/Q2/Q3 用户决策仍待回复

## 第三轮架构评审（软件团队·架构师高见远，只读，未动代码）
- 触发：用户问刚优化后架构还需不需要再优化；本环境 software-architect 专用 agent 不可用 → 改用 general-purpose 承载该角色
- 交付：`docs/arch-review-2026-08-20-round3.md`（实地读码验证 P0/P1/P2 落地 + 维度评分 + 残余债 R1-R9 + 反对过度工程清单）
- **结论**：地基已稳，不需大改，无危机债；综合 ≈7.0/10（分层8/线程8/编码铁律9/依赖启动5/可维护性6/一致性6）
- 上一轮真 bug 经读码确认全落地：跨 tab 互杀、NVENC 回退收窄、face_detection 崩溃、audio_mix 停止复位
- **重要硬伤（与完成报告口径不符）**：P1.5「requirements 校准」实际未落地——`requirements.txt` 仍强制 torch/demucs、且缺 `sherpa-onnx` 声明（core/sherpa_asr.py:12 顶层 import 它）→ 换机装字幕 SenseVoice 会 import 失败（唯一"安装/可复现"风险）。对应 R1
- 该做（多低优先）：R1 校准 requirements（阻 Q2）/ R2 D:\Models+端口50051约10处收口 model_dirs / R3 crf 默认20→23对齐 / R5 字幕 Whisper 独立进程纳入 track_proc / R7 ASR 收敛删 sherpa（阻 Q1）
- 暂缓：R4 worker run()/work() 约定未贯彻(11/14覆盖run()当前无bug) / R6 OllamaChecker 裸QThread / R8 taobao 1437行巨石 / R9 轻量冒烟脚本
- 不该做（过度工程）：ConfigStore/DI插件化/Web微服务/重型CI/asyncio重写线程/动业务铁律
- Q1(用哪几个ASR)/Q2(torch改可选)/Q3(crf默认对齐，澄清 video_enhance 走云端Wink 不调ffmpeg 故"没走get_encoder"不成立) 仍待用户回复
- 未动任何代码，无需重启

## R1 落地：依赖清单收拾干净（用户指令"先做 R1"）
- commit de6aee8（已 push，1ee2956→de6aee8）。仅改依赖清单文件，未动任何源码，无需重启
- 实地扫描：AST 全仓第三方模块 + 关键 import 路径确认（sherpa_asr 仅 subtitle_tab:68 函数内懒加载；torch/demucs/cosyvoice/whisper 全懒加载；requests 用于 kaipai+taobao 懒加载；playwright 仅 taobao 懒加载；sdk 仅 kaipai 懒加载；websocket 只在 cdp_fetch/cdp_read 独立调试脚本、应用不引用）
- **requirements.txt（核心）**：补 3 个漏声明 → sherpa-onnx(sherpa_asr.py:12 顶层 import，字幕必须)、opencv-python(cv2 人脸/截图)、requests(开拍云+淘宝)；移除 torch/torchaudio/demucs 强制；paddle 保留核心（文字识别为常用功能，且已懒加载不拖启动）
- **requirements-optional.txt（新建）**：torch、torchaudio、cosyvoice、demucs、openai-whisper、playwright(+playwright install 提示)、开拍云 sdk(注释，包名按官方文档)
- **消除的真实风险**：换机/新环境 `pip install -r requirements.txt` 后，字幕、人脸、截图、开拍云、淘宝下载不再因缺包 import 失败；重型 torch 链不再强制拖入主安装（规避此前 torch DLL/段错误老坑）
- 提交仅含两个依赖文件（干净提交）；config.json/MEMORY.md 等其它未暂存改动**未捎带**，保持 R1 范围
- 仍待用户答复 Q2（paddle 是否也降可选，当前保守保留核心）等；R2/R3/R5/R7 属后续清理

## video_concat filter_complex 绑定失败（截图报错修复）
- 用户截图报错：`Error binding filtergraph inputs/outputs: Invalid argument`
- 根因：P0/P1 重构（commit 4608376）切到 run_ffmpeg_with_fallback 时，`_build_concat_cmd` 漏写 `-i` 输入声明；filter 链按 `[0:v]/[1:v]/[2:v]/[1:a]/[2:a]` 引用 ffmpeg inputs 但 ffmpeg 拿不到对应 stream
- 修复：`core/video_concatenator.py` 的 `_build_concat_cmd` 按 `cover_video` 是否有，加 `-i cover_video -i video_a -i video_b`（有封面）或 `-i video_a -i video_b`（无封面），顺序与 filter 索引一致
- 冒烟测试 3 场景全过：无封面 / front 抽帧 / back folder 空列表
- commit e013eb4 已落本地；push 失败（github.com TCP 443 阻，DNS 正常），用户网络恢复后需手动 push
